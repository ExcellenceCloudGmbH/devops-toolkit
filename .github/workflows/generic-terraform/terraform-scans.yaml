name: checkov + trivy combined

on:
  workflow_call:

permissions:
  contents: read
  pull-requests: write

jobs:
  checkov-scan:
    uses: ./.github/workflows/generic-terraform/checkov.yaml

  trivy-scan:
    uses: ./.github/workflows/generic-terraform/trivy.yaml
